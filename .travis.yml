language: python
python:
  - "2.7"
  - "nightly"
# command to install dependencies
#install:
#  - pip install -r requirements.txt
# command to run tests
script:
  - pwd
  - ls -la
  - cp tests/uploadr.ini .
  - cat uploadr.ini
  - pytest # or py.test for Python versions 3.5 and below
# is this commmand necessary or if you have test_*.txt it will automatically run?
  - pytest --doctest-modules # Attempt to run test_*.txt with doctest
# force running 
  - pytest --doctest-modules test_isThisStringUnicode.txt # Attempt to run test_*.txt with doctest
  - pytest --doctest-modules test_isThisStringUnicode2.txt # Attempt to run test_*.txt with doctest
